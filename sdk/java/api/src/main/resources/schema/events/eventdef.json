{
  "type": "object",
  "javaType": "io.cncf.serverlessworkflow.api.events.EventDefinition",
  "properties": {
    "name": {
      "type": "string",
      "description": "Event Definition unique name",
      "minLength": 1
    },
    "source": {
      "type": "string",
      "description": "CloudEvent source UUID"
    },
    "type": {
      "type": "string",
      "description": "CloudEvent type"
    },
    "correlationToken": {
      "type": "string",
      "description": "Context attribute name of the CloudEvent which value is to be used for event correlation"
    },
    "kind": {
      "type" : "string",
      "enum": ["consumed", "produced"],
      "description": "Defines the events as either being consumed or produced by the workflow. Default is consumed",
      "default": "consumed"
    },
    "metadata": {
      "$ref": "../metadata/metadata.json"
    }
  },
  "if": {
    "properties": {
      "kind": {
        "const": "consumed"
      }
    }
  },
  "then": {
    "required": [
      "name",
      "source",
      "type"
    ]
  },
  "else": {
    "required": [
      "name",
      "type"
    ]
  }
}